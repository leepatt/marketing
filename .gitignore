# secrets / env
.env
.env.*

# os / editor
.DS_Store
Thumbs.db
desktop.ini

# node (if any tooling is added later)
node_modules/

# large media — media lives in Google Drive, not in this repo
*.mp4
*.mov
*.png
*.jpg
*.jpeg
*.psd

# ig-collect: never commit login session/cookies or scraped output
tools/.ig-session.json
tools/collected/
